# Task Brief: Adaptasi Maglo UI Kit (Figma) ke Admin SPMB

## Objectives
- Create `src/components/admin/MagloStatCard.tsx` (Maglo-style stat card with icon pills, metric counters, percentage badges, and visual progress bar).
- Create `src/components/admin/ApplicantChart.tsx` (Maglo-style bar chart component for applicant analytics).
- Create `src/components/admin/QuickActionDeck.tsx` (Maglo-style right side deck for quick verification & actions).
- Redesign `src/app/admin/(dasbor)/page.tsx` using Maglo 1440px dashboard layout grid.
- Verify with `npm test` (33 unit tests pass) and `npm run build`.
