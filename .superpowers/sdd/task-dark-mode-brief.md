# Task Brief: Fitur Light & Dark Mode Maglo UI Console

## Objectives
- Create `src/components/admin/ThemeToggle.tsx` (Client component with sun/moon icon, localStorage persistence, and html class toggle).
- Add `ThemeToggle` button to `src/components/admin/AdminHeader.tsx`.
- Update `src/app/globals.css` with dark mode support.
- Add Tailwind `dark:` variant classes across admin components (`MagloStatCard`, `ApplicantChart`, `QuickActionDeck`, `pendaftar/page.tsx`, `pendaftar/[id]/page.tsx`, `konten/page.tsx`).
- Verify with `npm test` (33 unit tests pass) and `npm run build`.
